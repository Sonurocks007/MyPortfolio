{
  "name": "my-portfolio-root",
  "version": "1.0.0",
  "description": "Monorepo root for Sonu Gupta Portfolio website",
  "private": true,
  "scripts": {
    "vercel-build": "npm run build:frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npx -y concurrently --kill-others -n \"frontend,backend\" -c \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  }
}
